# node-linker=hoisted is required for electron-forge to package the app correctly.
# Forge's module resolution is naive — it crawls node_modules on disk directly and
# does not support pnpm's default symlinked dependency layout.
# See: https://www.electronforge.io/cli#package
node-linker=hoisted
engine-strict=true
